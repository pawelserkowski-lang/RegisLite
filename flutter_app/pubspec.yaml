name: regis_lite_gui
description: A new Flutter GUI for RegisLite.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  file_picker: ^6.1.1
  flutter_markdown: ^0.6.18+1
  google_fonts: ^6.1.0
  provider: ^6.0.5
  universal_io: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
